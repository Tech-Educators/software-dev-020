export default function Page() {
    // set up database connection
    // get posts

    // once you have the posts -> map through them
    // in the return stament I'll map through each of my posts and display a link to that post 
    // `/posts/${post.id}`
    // THis is the heading -> this-is-the-heading - Use string + array methods
    // /posts/this-is-aslug-of-the-post-heading
    return (
        <div></div>
    )
}