"use client"
// import { connect } from "../utilities/connect"

import { connect } from "@/utilities/connect"

export default function DeleteButton() {
    const db = connect()
    return (
        <div>x</div>
    )
}